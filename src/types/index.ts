export interface Note {
    id: string;
    pitch: number;
    onset: number;
    duration: number;
}
export interface Dimensions {
    width: number;
    height: number;
}

export interface Position {
    x: number;
    y: number;
}
